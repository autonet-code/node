{"outcome": {"accepted": 0.0, "built_on": 0.0, "kept": 1.0, "paid": 0.0}, "problem": "CONTEXT \u2014 Designing the SECRET-ALLOWANCE + TRIPWIRE track for the Autonet ATN daemon (C:\\code\\autonet\\atn),\nNOW THAT PER-PID AGENT ISOLATION IS COMPLETE (phases 0-6 shipped). Threat model: THE AGENTS THEMSELVES (a\nmisaligned/compromised agent trying to read secrets it wasn't granted, or read another agent's grant). Each\nagent runs in its OWN OS process (own PID, isolated address space) \u2014 so kernel PID is now a REAL security\nidentity, and PID-bound access control is meaningful.\n\nWHAT ISOLATION AL", "resolution": "I have all four inputs (map, three design angles, red-team). This is synthesis only \u2014 no new exploration needed. Producing the owner-approval artifact.\n\n---\n\n# ATN SECRET-ALLOWANCE + TRIPWIRE \u2014 Canonical Design (owner review)\n\n## Executive summary (end state)\nEach agent runs in its own PID; the daemon computes a fractal, monotonically-narrowing secrets allowance at spawn (`L_child = resolve_spec(requested) \u2229 L_parent`) and, at PID-bind, mints an owner-secret-gated broker nonce carrying that allowance and registers `{child_pid \u2192 policies}` with the unchanged kevin vault-broker. The worker then calls `secret_*` tools from its OWN PID; the broker authorizes by kernel PID and stages values to nameless files, returning only `{var_name, path}` \u2014 the value never enters model context or daemon memory. A daemon-level `SecurityMonitor` holds only a LIVE-exposure set (fed at stage, drained at revoke + TTL), scans `STEP_OUTPUT`, and emits `SECURITY_ALARM` (names-only, `source_agent` only, no `agent_id` key). Every gate is fail-closed: broker down / no grant / unknown service / isolation-off / missing monitor \u2192 deny; the monitor never holds a standing vault copy.\n\n---\n\n## Canonical decisions (stated once)\n\n### 1. AllowanceGrant type + algebra\n- `AllowanceGrant = _ALLOWANCE_ALL (sentinel object) | frozenset[str]` where the frozenset is **resolved vault service names**. `_ALLOWANCE_ALL` = \"no upper bound\" (root); it is NEVER a child's *enforced* grant, only a *parent's* value inside INTERSE"}