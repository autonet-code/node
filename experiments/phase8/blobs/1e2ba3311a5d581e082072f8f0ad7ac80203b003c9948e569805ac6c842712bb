{"outcome": {"accepted": 0.0, "built_on": 0.0, "kept": 1.0, "paid": 0.0}, "problem": "USE WEBSEARCH AND WEBFETCH. Today is 2026-04-25.\n\nI'm building a TypeScript bridge that wraps the Gemini CLI (v0.7.0, package @google/gemini-cli) for programmatic use. Need to know which integration path is best.\n\nThe CLI has these flags relevant to programmatic use:\n- `-p / --prompt` \u2014 non-interactive single-prompt mode\n- `--output-format json` \u2014 JSON output\n- `--experimental-acp` \u2014 \"Starts the agent in ACP mode\"\n- `-y / --yolo` \u2014 auto-approve\n- `--approval-mode` \u2014 default | auto_edit | yolo\n\nQ", "resolution": "I'll research ACP and the Gemini CLI integration paths. [tool ToolSearch] I'll load WebSearch and WebFetch tools, then research. [tool ToolSearch] [tool WebSearch] [tool WebSearch] [tool WebFetch] [tool WebFetch] [tool WebSearch] [tool WebFetch] [tool WebFetch] [tool WebFetch] I have enough information now. The reminder isn't relevant to this research task. Let me get one more piece on session resume / streaming specifics. [tool WebFetch] [tool WebSearch] I have enough. Synthesizing the answer.\n\n---\n\n## Gemini CLI integration paths \u2014 recommendation\n\n### 1. What ACP is\n**Agent Client Protocol** (agentclientprotocol.com) \u2014 an open spec originated by Zed for editor/agent interop. JSON-RPC 2.0 over stdio with NDJSON framing. Same family as LSP, not the same as Claude Agent SDK's bespoke JSONL or Codex's MCP-shaped protocol \u2014 but conceptually equivalent: long-lived subprocess, request/response + notifications. The `--experimental-acp` flag on `gemini` starts the CLI as an ACP **server**; your bridge is the **client**.\n\n### 2. Long-lived subprocess: yes\nACP is explicitly designed for that. Implementation lives at `packages/cli/src/acp/acpClient.ts`. Zed and IntelliJ both use it exactly this way (one persistent `gemini --experimental-acp` per panel). Spawning fresh per prompt would defeat the point.\n\n### 3. Sessions\nStable methods: `initialize`, `authenticate`, `newSession`, `loadSession`, `prompt`, `cancel`, `setSessionMode`. `loadSession` resumes a prior session by id \u2014 so yes, mu"}