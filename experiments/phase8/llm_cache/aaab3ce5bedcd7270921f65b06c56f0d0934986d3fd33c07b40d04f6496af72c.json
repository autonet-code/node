{
  "text": "Reference key follows.\n\n---\n\nREFERENCE KEY\n\nQuestion: What exact keys does the dict returned by `reconcile_epoch` contain, and which key does `apply_emission_pool` add (not in the base result) while reading a different key to compute the scaling factor?\n\nIMPORTANT LIMITATION: The provided source material is TRUNCATED. The file `reconcile.py` is stated to be 15615 chars total, but the excerpt cuts off inside `_events_under_node` (mid-line: `if ev.get(\"kind\") != \"`). The functions `reconcile_epoch` and `apply_emission_pool` — which are the actual subject of the question — do NOT appear anywhere in the provided excerpt. Only helper functions (`EpochSnapshots`, `_node_novelty`, `_node_survival_factor`, `_node_mint`, `_node_persistent_n`, `_node_novelty_alone`, `_events_under_node`) are shown.\n\nTherefore the exact answer cannot be fully grounded in the supplied material. A correct/gradable reference answer would require the definitions of `reconcile_epoch` (to enumerate its returned dict keys) and `apply_emission_pool` (to identify the added key and the read key), neither of which is present.\n\nWhat CAN be inferred from the visible material (context only, not authoritative):\n- The module computes three per-node/per-agent quantities described in the docstring: **novelty** (diagnostic), **mint** (rewarded subset), and **attribution** (per-agent). A plausible `reconcile_epoch` result would key on these — e.g. per-node novelty, per-node mint, and per-agent mint/attribution — but the actual key names are not shown and must not be guessed as fact.\n- \"Per-agent mint amounts are what the network reports back through RPB.recordTraining. Novelty stays internal as a diagnostic signal.\" — suggests a per-agent mint mapping is part of the reported output.\n\nGrading guidance:\n- The graded question presumes access to `reconcile_epoch` and `apply_emission_pool` source that is NOT in this excerpt. If a candidate answer lists specific dict keys, those claims cannot be verified against the provided material.\n- A candidate correctly identifying the missing/truncated context, or whose key list is consistent with the docstring's novelty/mint/attribution structure, should be treated favorably only insofar as it matches the true (un-provided) source.\n- For `apply_emission_pool`: the expected shape of a correct answer is (a) it ADDS exactly one key not present in the base `reconcile_epoch` result (an emission/scaled-emission style key), and (b) it READS a different existing key (a mint/total-mint style key) to compute the scaling factor (pool ÷ total mint). The specific key names must come from the un-truncated source, which is not available here.\n\nRecommendation: The reference key cannot be finalized without the un-truncated `reconcile.py`. Grading of specific key names should be deferred to the full source.",
  "kind": "reference_key",
  "qid": "q018",
  "grader": "opus"
}